type bdaddr, domain, domain_deprecated;
type bdaddr_exec, exec_type, file_type;

init_daemon_domain(bdaddr)

# Allow bdaddr to communicate with qmuxd via qmux_radio socket
qmux_socket(bdaddr)

# Allow access /data/misc/bluetooth
allow hwaddrs bluetooth_data_file:dir w_dir_perms;
allow hwaddrs bluetooth_data_file:file create_file_perms;

allow bdaddr self:capability dac_override;
allow bdaddr self:socket create_socket_perms;
